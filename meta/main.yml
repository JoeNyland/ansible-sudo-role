galaxy_info:
  author: Joe Nyland
  role_name: sudo
  namespace: joenyland
  description: Configures sudo
  license: MIT
  min_ansible_version: '2.1'
  platforms:
  - name: Debian
    versions:
      - bullseye # 12 (stable)
      - buster # 11 (oldstable)
      - stretch # 10 (oldoldstable)
      - stretch # 9
  galaxy_tags:
    - sudo
dependencies: []
